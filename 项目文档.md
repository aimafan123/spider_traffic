# Spider Traffic 项目文档

> 本文档基于对仓库全部源码、配置、构建脚本与运行时产物的逐文件阅读编写，所有结论均可回溯到具体文件与行号。
> 与仓库中已有的 `README.md`、`技术文档.md`、`项目分析报告.md` 相比，本文档以**当前代码的真实行为**为准，并在 [15.4 对既有文档的勘误](#154-对既有文档的勘误) 中列出旧文档与代码不一致之处。

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 编写日期 | 2026-09-17 |
| 对应代码版本 | `6b48f26`（`feat: add edge and firefox spider support`，2026-03-19），工作区含 `action.py`、`.gitignore`、`README.md` 未提交改动 |
| 代码规模 | Python 22 个文件，约 2436 行（含注释与空行） |
| 文档语言 | 中文 |
| 适用读者 | 使用者、运维、二次开发者、网络测量研究者 |

---

## 目录

- [1. 项目概述](#1-项目概述)
- [2. 仓库结构与目录约定](#2-仓库结构与目录约定)
- [3. 系统架构](#3-系统架构)
- [4. 运行流程逐步拆解](#4-运行流程逐步拆解)
- [5. 模块详解](#5-模块详解)
- [6. 配置参考](#6-配置参考)
- [7. 数据产物与格式](#7-数据产物与格式)
- [8. 部署与运行](#8-部署与运行)
- [9. 流量解码原理](#9-流量解码原理)
- [10. 验证记录](#10-验证记录)
- [11. 已知问题与风险清单](#11-已知问题与风险清单)
- [12. 排障手册](#12-排障手册)
- [13. 二次开发指南](#13-二次开发指南)
- [14. 安全与合规](#14-安全与合规)
- [15. 附录](#15-附录)

---

## 1. 项目概述

### 1.1 项目定位

Spider Traffic 是一个**浏览器驱动的加密流量采集与分析框架**，而不是传统意义上的数据爬虫。

它的目标产物不是抓取到的业务数据，而是可复现、可离线分析的网络测量证据：

1. 真实浏览器访问网站产生的原始报文（`pcap`）；
2. 浏览器导出的 TLS 会话密钥（`SSLKEYLOGFILE`）；
3. 页面渲染截图；
4. 基于密钥离线解密后重建的资源级请求/响应清单（JSON）。

因此，项目中的 Scrapy 只承担"URL 调度 + 页面内链接发现"的角色，真正的页面加载由 Selenium 驱动的真实浏览器完成（`spider/middlewares.py` 中的下载中间件直接返回 `HtmlResponse`，网络请求根本不经过 Scrapy 的下载器）。

### 1.2 核心能力

| 能力 | 实现位置 | 状态 |
| --- | --- | --- |
| 真实浏览器访问站点 | `spider/chrome.py`、`spider/edge.py`、`spider/firefox.py` | 可用（三浏览器） |
| 站内链接深度爬取 | `spider/spiders/trace.py` + `middlewares.py` | 可用（受 `webnum` / 白名单域名限制） |
| `tcpdump` 抓包 | `traffic/capture.py` | 可用 |
| 浏览器 TLS 密钥落盘 | `main.py`（`SSLKEYLOGFILE`）+ `_save_sslkey_files()` | 可用 |
| 直连模式 HTTP/1.1 + HTTP/2 解码 | `tls_decoder/http2decoder.py` | 可用（已实测） |
| Xray(Trojan) 双层解密 | `tls_decoder/TrojanDecoder.py` | 实验性，可运行但强依赖手工维护的配置 |
| Tor 通道采集 | `torDo.py` + `main.py` | 仅采集，无配套解码 |
| 代理启动/停止编排 | `main.py`（Xray）、`torDo.py`（Tor） | 可用 |
| 任务索引持久化、多轮循环 | `main.py` + `spider/task.py` | 可用（文件级、单实例） |
| 自动截图 | `spider/middlewares.py` | 代码可用，历史产物目录中未留存 |
| 单元/集成测试 | 无 | 缺失 |

### 1.3 能力边界（明确的"不做"或"未完成"）

- **没有分布式调度**：任务状态只保存在 `config/running.json` 的一个整数下标里。
- **URL 输入是"主机名"而不是完整 URL**：`spider/task.py:53` 内部硬编码拼接 `https://`，配置里写完整 URL 会被拼成 `https://https://...`。
- **Tor 模式没有解码链路**：`main.py:72-74` 中 `tor` 分支直接返回 `True`。
- **HTTP/1.1 端口 80 的明文流量被显式排除**（`capture.py` 过滤 `not port 80`），因此解码结果只有 HTTPS。
- **QUIC/HTTP3 不参与解码**：默认通过浏览器参数关闭 QUIC，使流量回落到 TLS over TCP。
- **Playwright / Splash 未接入运行路径**：`requirements.txt` 中存在 `playwright`、`scrapy-playwright`、`scrapy-splash`，但 `src/` 中没有任何引用（已用全量 import 扫描确认）。
- **没有可运行的自动化测试**：`test/` 目录只是样例数据（`test.pcap`、`sslkeys.log`、`results.json`），仓库没有 `tests/`，`pytest` 也不在依赖中。
- **`config/` 与 `data/` 属于环境资产**，被 `.gitignore` 排除，克隆仓库后必须自行准备（见 [第 6 章](#6-配置参考)、[第 8 章](#8-部署与运行)）。

### 1.4 技术栈

运行时逐模块统计到的**实际第三方依赖**（通过 `grep` 全量 import 得到）：

| 依赖 | 版本（requirements.txt） | 用途 |
| --- | --- | --- |
| Scrapy | 2.12.0 | 爬虫引擎、调度、日志 |
| Selenium | 4.26.1 | 浏览器自动化 |
| pyshark | 0.6 | 调用 tshark 做 TLS/HTTP 解码 |
| scapy | 2.6.1 | Trojan 内层报文重建 |
| Twisted | 24.10.0 | Scrapy 事件循环（asyncio reactor） |
| itemadapter / parsel / w3lib / lxml | — | Scrapy 传递依赖 |

`requirements.txt` 中**未被任何源码引用**的包（依赖膨胀，可裁剪）：
`playwright`、`scrapy-playwright`、`scrapy-splash`、`beautifulsoup4`/`bs4`、`tldextract`、`paramiko`、`scp`、`termcolor`、`pytils`、`requests`、`cryptography`、`pyOpenSSL`、`sniffio`、`sortedcontainers` 等（`cryptography`/`pyOpenSSL` 由 Twisted/Selenium 间接需要，删除前需验证）。

外部二进制工具（非 Python）：

| 工具 | 当前环境实测版本 | 用途 |
| --- | --- | --- |
| `tcpdump` | 4.99.4 / libpcap 1.10.4 | 抓包 |
| `tshark` | 4.4.9 | TLS 解密与 HTTP 解析 |
| Xray | 24.11.11 | `xray` 模式代理 |
| Tor | 0.4.7.13 | `tor` 模式代理 |
| Chrome / Edge / Firefox + 各自 driver | 随 `bin/` 分发 | 浏览器访问 |

---

## 2. 仓库结构与目录约定

### 2.1 完整源码树

```text
spider_traffic/
├── action.sh                     # 本地启动脚本：cd src && ../.venv/bin/python3 -m spider_traffic.main
├── README.md                     # 项目介绍（英文）
├── AGENTS.md                     # 仓库协作规范
├── CHANGELOG.md                  # 更新日志
├── 技术文档.md / 项目分析报告.md   # 既有文档（已被 .gitignore 忽略）
├── 项目文档.md                    # 本文档
├── Dockerfile                    # 主构建文件（Ubuntu + 三浏览器）
├── dockerfiles/                  # ubuntu20 / ubuntu24 / debian12 三个变体
├── requirements.txt              # Python 依赖（Ubuntu 24 主线）
├── requirements/requirements_ubuntu20.txt
├── .gitignore / .gitattributes / .dockerignore
├── config/                       # 运行配置（git 忽略，见 2.3）
│   ├── config.ini                #   主配置
│   ├── current_docker_url_list.txt          #   当前启用的 URL 列表（主程序读取）
│   ├── current_docker_url_list_background.txt  #   9419 行，当前无代码引用
│   ├── current_docker_url_list_monitor.txt     #   99 行，当前无代码引用
│   ├── exclude_keywords          #   链接过滤关键字
│   ├── running.json              #   当前任务下标（程序回写）
│   ├── xray_trojan.json          #   Trojan 出口配置（含 masterKeyLog）
│   └── xray_ss.json              #   Shadowsocks 出口配置
├── data/                         # 运行产物（git 忽略）
│   └── pcap/<host>/*.pcap        #   当前实际存在的只有 pcap
├── logs/                         # 运行日志（git 忽略）
│   ├── defult.log                #   主程序 + 解码日志（RotatingFileHandler）
│   ├── url.log                   #   逐请求 URL 记录
│   └── scrapy_YYYY_M_D.log       #   Scrapy 日志（按天）
├── bin/                          # 运行时二进制（git 忽略，但 Docker 构建必需）
│   ├── Xray-linux-64/xray        #   Xray 24.11.11
│   ├── tor_s/tor 等              #   Tor 0.4.7.13 + obfs4/meek/snowflake/webtunnel
│   ├── chromedriver-linux64/chromedriver
│   ├── msedgedriver / geckodriver
│   └── google-chrome-*.deb / microsoft-edge-*.deb / firefox_*.deb
├── test/                         # 样例数据（git 忽略，test*）
│   ├── test.pcap                 #   203 MB 实测抓包（同时含 HTTP/1.1 与 HTTP/2）
│   ├── sslkeys.log               #   对应 TLS 密钥
│   └── results.json              #   历史解码结果（67 流 / 77 资源）
└── src/
    ├── scrapy.cfg                # settings 模块指向 spider_traffic.spider.settings
    └── spider_traffic/
        ├── main.py               # 总控编排（无限循环 + 进程管理 + 解码调度）
        ├── action.py             # 单站任务子进程入口（抓包 + Scrapy）
        ├── torDo.py              # Tor 启停
        ├── myutils/
        │   ├── __init__.py       #   project_path 计算
        │   ├── config.py         #   config.ini 读取 + SPIDER_MODE
        │   └── logger.py         #   defult.log / url.log 两个 logger
        ├── spider/
        │   ├── __init__.py       #   kill_browsers() 浏览器清进程分发
        │   ├── task.py           #   Task 单例（URL 列表、下标、过滤词）
        │   ├── settings.py       #   Scrapy settings
        │   ├── middlewares.py    #   Selenium 下载中间件（核心）
        │   ├── items.py          #   空壳
        │   ├── pipelines.py      #   空壳
        │   ├── chrome.py         #   Chrome driver + 滚动 + 进程清理
        │   ├── edge.py           #   Edge driver + 进程清理
        │   ├── firefox.py        #   Firefox driver + 进程清理
        │   └── spiders/
        │       ├── __init__.py
        │       └── trace.py      #   唯一 Spider，负责链接发现
        ├── traffic/
        │   └── capture.py        #   tcpdump 抓包
        └── tls_decoder/
            ├── flow_key.py       #   方向无关的五元组键
            ├── http2decoder.py   #   HTTP/1.1 + HTTP/2 解码器
            └── TrojanDecoder.py  #   Trojan 外层剥离 + 内层二次解码
```

### 2.2 `project_path` 的推导方式

`myutils/__init__.py:3` 用四层 `dirname` 从 `myutils/__init__.py` 反推仓库根：

```python
project_path = os.path.dirname(os.path.dirname(os.path.dirname(os.path.dirname(
    os.path.abspath(__file__)))))
# myutils → spider_traffic → src → <repo root>
```

所有配置、日志、数据、`bin/` 的定位都基于它。**因此源码目录层级不可随意改动**，否则路径全部失效。

### 2.3 `.gitignore` 与运行资产的边界

```text
bin/*            # 二进制不入库（仅保留 .gitkeep）
config/*         # 全部配置不入库（仅 .gitkeep），config/xray.json 单独再忽略一次
data/*           # 产物不入库
logs/*           # 日志不入库
test*            # test/ 与 test.json 不入库
*.tar
项目分析报告.md / 技术文档.md   # 本次未提交改动新增
```

含义：**克隆仓库后程序无法直接运行**，必须补齐 `config/config.ini`、`config/current_docker_url_list.txt`、`config/exclude_keywords`，以及 `bin/` 下的浏览器/驱动/代理二进制。`bin/` 中的 `.deb` 通过 `.gitattributes` 标记过 Git LFS（`bin/google-chrome-stable_current_amd64.deb filter=lfs`），但当前 `bin/*` 被忽略，说明这些文件实际靠本地或离线包分发。

---

## 3. 系统架构

### 3.1 分层视图

```text
┌──────────────────────────────────────────────────────────────────────┐
│ 编排层  main.py                                                       │
│   · 校验 mode / browser                                               │
│   · 无限循环：取当前 URL → 抓包 → 起代理 → 起子进程 → 收尾 → 解码      │
│   · SSLKEY 归档、running.json 轮转                                    │
└───────┬───────────────────────────┬──────────────────────┬───────────┘
        │ subprocess                │ Popen                │ terminate
        ▼                           ▼                      ▼
┌──────────────────┐   ┌────────────────────┐   ┌──────────────────────┐
│ action.py 子进程 │   │ Xray / Tor 进程    │   │ tcpdump 进程         │
│  · capture()     │   │ (xray|tor 模式)    │   │ traffic/capture.py   │
│  · CrawlerProcess│   └────────────────────┘   └──────────┬───────────┘
└────────┬─────────┘                                       │
         │ Scrapy 引擎                                      ▼
         ▼                                        data/pcap/<host>/*.pcap
┌──────────────────────────────────────────────┐
│ spider/middlewares.py :: DownloaderMiddleware│
│   · 计数 webnum，超限抛 IgnoreRequest         │
│   · browser.get(url)                         │
│   · scroll_to_bottom()（chrome.py）           │
│   · save_screenshot()                        │
│   · 返回 HtmlResponse(page_source)           │
└────────┬─────────────────────────────────────┘
         │ Selenium WebDriver
         ▼
┌──────────────────────────────────────────────┐
│ Chrome / Edge / Firefox（headless）           │
│   · SSLKEYLOGFILE=/tmp/sslkeys.log           │
│   · xray: http 代理 / tor: socks5 代理        │
└──────────────────────────────────────────────┘
         │ 采集结束
         ▼
┌──────────────────────────────────────────────┐
│ tls_decoder                                  │
│   http2decoder.py  →  direct 模式 JSON       │
│   TrojanDecoder.py →  xray(trojan) 模式 JSON │
└──────────────────────────────────────────────┘
```

关键设计点：**Scrapy 不参与真实网络请求**。`SpiderDownloaderMiddleware.process_request()`（`middlewares.py:117`）直接用 Selenium 打开页面并把 `page_source` 包装成 `HtmlResponse` 返回，下载器链路被短路，因此 `DOWNLOAD_DELAY`、`CONCURRENT_REQUESTS`、`ROBOTSTXT_OBEY` 等 Scrapy 设置对"页面加载"本身几乎没有实际约束力（`DOWNLOAD_DELAY` 只影响请求之间的调度间隔）。

### 3.2 进程模型

一次完整的 URL 采集涉及以下进程/线程：

| 进程/线程 | 创建者 | 生命周期 |
| --- | --- | --- |
| `main.py` 主进程 | 用户 / 容器 | 常驻，`while True` |
| `action.py` 子进程 | `main.py:161` `subprocess.run` | 单 URL，爬取结束即退出 |
| `tcpdump` 进程 | `traffic/capture.py:49` `Popen` | 单 URL，`terminate()`/`kill()` |
| `Xray` 进程 | `main.py:207` `Popen`（仅 xray 模式） | 单 URL |
| `Tor` 进程 | `torDo.py:52` `Popen`（仅 tor 模式） | 单 URL |
| 浏览器 + driver | Selenium（action 子进程内） | 单 URL，由 `kill_browsers()` 强杀 |
| 定时器线程 | `action.py:42` `threading.Timer` | 单 URL，`time_per_website` 秒后触发 |
| Tor 引导等待子进程 | `torDo.py:21` `multiprocessing.Process` | 最多 300 秒 |
| Scrapy 日志/统计 | Twisted reactor | 单 URL |

要点：

- 浏览器运行在 `action.py` 子进程中，但 `SSLKEYLOGFILE` 由 `main.py:169` 在主进程设置，通过环境变量继承下去，因此密钥文件写的是**主进程指定的路径** `/tmp/sslkeys.log`。
- 子进程与主进程之间只通过**文件系统**（pcap 路径、下标记）与子进程退出码通信，没有 RPC。
- 抓包在 Xray 启动**之前**开始（`main.py:201` 先 `traffic()`，`main.py:207` 再起 Xray），保证隧道建立阶段不丢包。

### 3.3 单 URL 采集时序

```text
时间轴（以 direct 模式为例，默认配置 time_per_website=30）
─────────────────────────────────────────────────────────────────────
t0        kill_browsers(browser)                    # 清理残留浏览器
t0+ε      tcpdump 启动（capture() 内 sleep 1）
t0+1s     启动 action.py 子进程（subprocess.run，阻塞主进程）
          ├─ CrawlerProcess 启动
          ├─ DownloaderMiddleware.__init__ 创建浏览器
          ├─ Timer(30s) 启动
          ├─ 请求 1：browser.get(start_url) → 滚动 → 截图 → HtmlResponse
          ├─ 解析链接，超过 webnum 的请求抛 IgnoreRequest
          └─ 30s 到点：crawler.stop() → 队列放 STOP → process.start() 返回
t0+31s    子进程退出，action_thread.join() 返回
          sleep(3)
          kill_browsers(browser)                    # 强杀浏览器
          sleep(30)                                  # 等尾部流量落地
          stop()：terminate tcpdump（+ Xray/Tor）
          _save_sslkey_files()                       # /tmp/sslkeys.log → *.log
          is_decode=true 时执行解码
          current_index = (current_index+1) % url_num
          写 config/running.json
─────────────────────────────────────────────────────────────────────
单站固定开销 ≈ time_per_website + 3 + 30 + 抓包/浏览器启动时间（约 65s 起）
tor 模式额外固定等待 60s（main.py:258）
仅 tor 模式在启动失败时会删除 pcap 并重试同一 URL（main.py:247-254）
xray 模式不校验 Xray 是否真的启动成功（stderr 被丢弃），失败也照常进入爬取
```

### 3.4 三种模式对比

| 维度 | `direct` | `xray` | `tor` |
| --- | --- | --- | --- |
| 代理 | 无 | Xray（http 代理） | Tor（socks5 代理） |
| 抓包过滤 | 全部（去掉 22/80 端口） | `host <ip_addr>`（只抓与代理服务器的流量） | 全部（去掉 22/80 端口） |
| 浏览器 SSLKEY | 写 `/tmp/sslkeys.log` | 写 `/tmp/sslkeys.log` | 写 `/tmp/sslkeys.log` |
| 代理侧密钥 | 不需要 | Xray `masterKeyLog` → `/tmp/xray_sslkeylog.log` | 无 |
| 归档文件 | `<pcap>.log` | `<pcap>.log` + `<pcap>.xray.log` | `<pcap>.log` |
| 解码 | HTTP/1.1 + HTTP/2（`http2decoder.py`） | 仅 `protocal=trojan` 时走 `TrojanDecoder.py` | 无 |
| 额外等待 | 无 | 无 | Tor 引导成功后固定 `sleep 60`（`main.py:258`） |
| 代理配置来源 | — | `config/xray.json`（**当前缺失**，需自行生成） | `config/torrc`（**当前缺失**） |
| 失败处理 | — | — | 删除 pcap 并重试当前 URL |

---

## 4. 运行流程逐步拆解

本章以 `main.py:browser_action()` 为主线，逐阶段说明"做了什么、依赖什么、失败会怎样"。

### 阶段 0：模块导入期的副作用

导入 `spider_traffic.main` 会依次触发：

1. `myutils/__init__.py` 计算 `project_path`；
2. `myutils/config.py:11` 读取 `config/config.ini`，并在 `config.py:13` 取 `SPIDER_MODE = config["spider"]["mode"]`；**配置文件缺失会直接抛 `KeyError`/`NoSectionError`**；
3. `myutils/logger.py:41-42` 创建 `defult` 与 `url` 两个 logger，并向 `logs/` 打开 `RotatingFileHandler`；**`logs/defult.log` 不可写会直接 `PermissionError`，程序无法启动**（实测现象见 [第 10 章](#10-验证记录)）；
4. `spider/task.py:61` 实例化单例 `task_instance`，读取 URL 列表、`running.json`、`exclude_keywords`。

也就是说，**在进入主循环前程序就已经强依赖 4 个文件**：`config/config.ini`、`config/current_docker_url_list.txt`、`config/running.json`、`config/exclude_keywords`，外加 `logs/` 可写。

### 阶段 1：环境与参数准备（`main.py:168-194`）

- 设置 `SSLKEYLOGFILE=/tmp/sslkeys.log`；
- 读取 `name`、`site`、`is_decode`、`browser`；
- 校验 `SPIDER_MODE ∈ {xray, tor, direct}`，否则 `ValueError`；
- 校验 `browser ∈ {chrome, edge, firefox}`，否则 `ValueError`；
- 按模式确定 `PROTOCAL_NAME`：
  - `xray` / `tor`：`config["information"]["protocal"]`；
  - `direct`：强制为字面量 `"direct"`；
- `xray` 模式下解析 `bin/Xray-linux-64/xray` 与 `config/xray.json`。

`PROTOCAL_NAME` 同时用于：pcap 文件名前缀、xray 解码分支判断（是否等于 `trojan`）。

### 阶段 2：开始抓包（`main.py:201` → `traffic/capture.py`）

`traffic()`（`action.py:57`）计算文件名与目录，然后调用 `capture(output_path)`：

| 模式 | 实际命令 |
| --- | --- |
| direct / tor | `tcpdump -n not port 22 and not port 80 -w <pcap>` |
| xray | `tcpdump -n host <ip_addr> and not port 22 and not port 80 -w <pcap>` |

实现细节与限制：

- 未指定 `-i`，使用 tcpdump 的默认网卡选择逻辑；
- 未指定 `-s`，使用默认 snaplen（现代 tcpdump 为 262144 字节，足够）；
- 未使用 `-U`/`--immediate-mode`，数据靠进程 `terminate()` 时的缓冲刷新落盘；
- `Popen` 的 stdout/stderr 全部丢到 `DEVNULL`，**抓包错误不可见**；
- 启动后 `time.sleep(1)` 再返回，给 tcpdump 留初始化时间（`capture.py:56`）；
- 需要 `CAP_NET_RAW`/root 权限；当前主机 `getcap $(which tcpdump)` 为空，即**必须 sudo 或容器 `--privileged`**。

### 阶段 3：启动代理（仅 xray / tor）

- **xray**：`subprocess.Popen([xray, "run", "--config", config/xray.json])`，三路标准流全部丢弃（`main.py:207-212`）。进程句柄保存在主循环内，URL 结束后终止。
- **tor**：`start_tor()`（`torDo.py:31`）
  1. 清空 `logs/notice.log`；
  2. 用 `bin/tor_s/tor -f config/torrc` 启动；
  3. `wait_for_100()` 起一个 `multiprocessing.Process`，每秒执行 `cat notice.log | grep 'Bootstrapped 100%' | wc -l`，最多等 300 秒；
  4. 返回 `(process, bool)`。

`torDo.py` 的两个已知问题：函数签名是 `start_tor()`（无参数），但 `__main__` 里写成 `start_tor("webtunnel")`（`torDo.py:77`），直接运行该文件会 `TypeError`；docstring 中描述的多种桥接方式（meek/obfs4/snowflake/webtunnel）在代码里**没有任何分支实现**。

### 阶段 4：浏览器爬取（`action.py` → Scrapy → 中间件）

`main.py:260` 起线程执行 `run_action_script(traffic_path)`，实际命令为：

```python
["../.venv/bin/python3", "-m", "spider_traffic.action", traffic_path]   # main.py:162
```

- 解释器路径是**写死的相对路径**，要求当前工作目录必须是 `src/`；
- `subprocess.run` 未检查返回码，子进程崩溃不会中断主循环；
- 位置参数 `traffic_path` 被 `action.py:77` 读取，并透传给 Spider 用于命名截图。

`action.py:35 start_spider()`：

1. `CrawlerProcess(get_project_settings())`；
2. `time_limit = int(config["spider"]["time_per_website"])`；
3. `process.crawl(Spider, pcap_path=pcap_path)`；
4. 启动 `threading.Timer(time_limit, stop_crawlers_after_delay)`（daemon）；
5. `process.start()` 阻塞直到引擎停止；
6. 通过 `stop_signal_queue` 确认停止信号。

`stop_crawlers_after_delay()`（`action.py:27`）遍历 `process.crawlers` 调用 `crawler.stop()`，再向队列投递 `"STOP"`。

中间件行为（`middlewares.py:117-150`）：

1. `task_instance.requesturlNum += 1`，超过 `max_webnum` 抛 `IgnoreRequest`（`webnum=-1` 时上限为 999999）；
2. `self.browser.get(request.url)`；
3. 写 `logs/url.log`；
4. `scroll == true` 时调用 `scroll_to_bottom()`；
5. 截图到 `data/screenshot/<url 去掉协议部分>/<pcap 文件名>.png`；
6. 返回 `HtmlResponse(url, body=page_source, encoding="utf-8")`。

`Spider.parse()`（`trace.py:22`）用 CSS 选择器取所有 `<a href>`，`urljoin` 成绝对地址，过滤掉 `x.com` 的 analytics 链接与 `exclude_keywords` 命中的 URL，然后 `response.follow` 递归。由于 `allowed_domains` 由 Scrapy 的 OffsiteMiddleware 强制，**跨站链接不会被跟随**（只抓站内页面）。

### 阶段 5：收尾与密钥归档（`main.py:266-275`）

1. `time.sleep(3)`；
2. `kill_browsers(browser_name)`：`pkill` 强杀 driver 与浏览器进程；
3. `time.sleep(30)`：等待连接关闭、慢请求收尾；
4. `stop()`：先终止代理（Xray `terminate`→5s 超时后 `kill`；Tor 同），再终止 tcpdump（同样 5s 超时后 `kill`）；tor 启动失败时删除 pcap；
5. `_save_sslkey_files(traffic_path)`：
   - `shutil.move("/tmp/sslkeys.log", "<pcap_basename>.log")`；
   - xray 模式下再 `shutil.move("/tmp/xray_sslkeylog.log", "<pcap_basename>.xray.log")`；
   - **注意**：若浏览器密钥文件不存在，函数在第 30-34 行直接 `return`，因此 **xray 密钥文件也不会被归档**。

### 阶段 6：解码（`main.py:278-279`）

仅当 `config["spider"]["is_decode"].strip().lower() == "true"` 时执行：

| 模式 | 分支 | 条件 | 产物 |
| --- | --- | --- | --- |
| direct | `_decode_direct_mode()` | `<pcap>.log` 与 pcap 都存在 | `<pcap>.json` |
| xray | `_decode_xray_mode()` | `protocal.lower() == "trojan"` 且 `.log` 存在 | `<pcap>.json` |
| tor / 其他 | 直接返回 `True` | — | 无 |

解码异常被 `try/except` 捕获并写日志，不会中断主循环。

### 阶段 7：索引轮转与循环

```python
task_instance.current_index = (task_instance.current_index + 1) % task_instance.url_num
# 写回 config/running.json: {"currentIndex": N}
```

随后 `while True` 进入下一轮。程序**没有正常退出路径**，只能 Ctrl-C / 杀进程 / 容器停止。URL 列表在进程启动时一次性读取，运行期修改列表文件不会生效（但 `task_instance` 对象会被下一轮复用，`requesturlNum` 也会在每轮子进程中重新计数，因为子进程是全新解释器）。

---

## 5. 模块详解

### 5.1 `main.py` — 总控编排层（291 行）

| 函数 | 行号 | 职责 |
| --- | --- | --- |
| `_save_sslkey_files(traffic_path)` | 19 | 把 `/tmp/sslkeys.log`、`/tmp/xray_sslkeylog.log` 移动到 pcap 同目录并改名 |
| `_process_traffic_decoding(...)` | 57 | 按模式分发解码 |
| `_decode_direct_mode(traffic_path)` | 77 | 构造 `TLSStreamDecoder`，写 `<pcap>.json` |
| `_decode_xray_mode(traffic_path, protocol_name)` | 120 | 仅处理 trojan，调用 `TrojanDecoder.action()` |
| `run_action_script(traffic_path)` | 161 | `subprocess.run` 启动 action 子进程 |
| `browser_action()` | 167 | 主循环 |

设计要点与坑：

- `_decode_xray_mode()` 要求 `<pcap>.log`（浏览器密钥）存在，但 Trojan 解码真正需要的是 `.xray.log`（外层密钥）。因此**缺少 xray 密钥时不会报错，只会得到一个解码失败的 JSON 或空结果**。
- 解码器 `TLSStreamDecoder` 在解密 HTTP/2 时**必须**配合浏览器密钥；即使 xray 模式，内层流量也是浏览器到目标站的 TLS，依赖的是 `<pcap>.log`。
- `main.py` 全程使用全局 `config` 对象与模块级 `SPIDER_MODE`，运行期无法热更新。

### 5.2 `action.py` — 单站任务执行器（78 行）

| 符号 | 行号 | 说明 |
| --- | --- | --- |
| `stop_signal_queue` | 19 | 模块级 `Queue`，跨中间件/定时器传递停止信号 |
| `_normalize_url_for_path(url)` | 22 | `url.split("//")[-1].replace("/", "_")`，把 URL 变成目录安全名 |
| `stop_crawlers_after_delay(process)` | 27 | 定时器回调：停止所有 crawler 并投递 STOP |
| `start_spider(pcap_path)` | 35 | 创建 `CrawlerProcess`、注册 Spider、起定时器、阻塞运行 |
| `traffic(VPS_NAME, PROTOCAL_NAME, SITE_NAME, url)` | 57 | 生成 pcap 路径并启动抓包，返回 `(process, path)` |

pcap 命名规则（`action.py:65-68`）：

```text
data/pcap/<normalized_url>/<PROTOCAL_NAME>_<YYYYMMDDHHMMSS>_<VPS_NAME>_<SITE_NAME>_<normalized_url>.pcap
例：data/pcap/baidu.com/direct_20260320170144_<node-ip>_us_baidu.com.pcap
```

日志中的"爬取时间设为 X 分钟"实际是把秒数除以 60 打印（`action.py:48`），单位表述有误，不影响行为。

### 5.3 `torDo.py` — Tor 管理（80 行）

| 函数 | 行号 | 说明 |
| --- | --- | --- |
| `long_running_task(log_file)` | 10 | 每秒轮询日志中 `Bootstrapped 100%` 的出现次数 |
| `wait_for_100(log_file, timeout=180)` | 19 | 起子进程等待，超时 terminate，返回布尔值；`start_tor` 传入 300 |
| `start_tor()` | 31 | 清空 `logs/notice.log` → 启动 `bin/tor_s/tor -f config/torrc` → 等待引导 |
| `close_tor(tor_process)` | 64 | 先 `terminate()`，5 秒未退出则 `kill()` |

依赖文件：`config/torrc`（**当前仓库不存在**）、`logs/notice.log`（首次运行由脚本创建）。
已知缺陷：轮询用 `os.popen("cat ... | grep ... | wc -l")`，每次创建 shell 进程，效率低但对本项目可接受；docstring 与实现不一致（见 [11 章](#11-已知问题与风险清单)）。

### 5.4 `traffic/capture.py` — 抓包层（84 行）

| 函数 | 行号 | 状态 |
| --- | --- | --- |
| `capture(output_path)` | 14 | **在用** |
| `stop_capture(formatted_time, TASK_NAME)` | 62 | **死代码/已损坏**：引用未定义的全局 `process`，且读取不存在的配置项 `config["spider"]["i2pd_path"]`；主程序改为直接 `terminate()` |
| `move_log(log_path, dst_path)` | 75 | 仅被 `stop_capture` 使用 |

`capture()` 返回 `Popen` 对象，调用方负责终止。由于 stdout/stderr 被丢弃，`tcpdump: permission denied` 之类的错误不会出现在日志中，只表现为"生成了 0 字节 pcap"。

### 5.5 `spider/task.py` — 任务与状态（61 行）

`Task` 是经典单例（`__new__` + `_initialized`），模块导入时即创建 `task_instance`。

| 成员 | 说明 |
| --- | --- |
| `file_path` | `config/current_docker_url_list.txt` |
| `urls` | `read_file()` 结果：去空行、去 `#` 注释行 |
| `url_num` | URL 条数 |
| `requesturlNum` | 本轮已发请求数（配合 `webnum`） |
| `current_index` | 从 `config/running.json` 读取的 `currentIndex` |
| `exclude_keywords` | 读 `config/exclude_keywords`，逐行 `s.replace("\n", " ")` |
| `current_start_url` | `"https://" + urls[current_index]` |
| `current_allowed_domain` | `urls[current_index]` 原样 |

**注意 `exclude_keywords` 的处理缺陷**：读取时把换行替换成空格，关键字会变成带尾随空格的字符串（如 `"ogin "`），导致 `trace.py:34` 的子串匹配只在关键字后紧跟空格时才生效（见 [11 章](#11-已知问题与风险清单)）。

### 5.6 `spider/spiders/trace.py` — 唯一的 Spider（39 行）

- `name = "trace"`；
- `allowed_domains`、`start_urls` 是**类属性**，在模块导入时由 `task_instance` 求值。因为每轮 `action.py` 都是新进程，所以能正确取到最新的 `current_index`；
- `parse()` 逻辑：取 `a::attr(href)` → `response.urljoin` → 只保留 `http/https` → 跳过 `analytics`+`x.com` → 跳过命中关键字的链接 → `response.follow`；
- 不产出 item，因此 `ITEM_PIPELINES` 即使启用也没有数据流。

### 5.7 `spider/middlewares.py` — 浏览器适配层（172 行）

`SpiderSpiderMiddleware`（23-67 行）是 Scrapy 模板残留，未在 `settings.py` 注册。
`SpiderDownloaderMiddleware`（70-172 行）是核心：

| 方法 | 行号 | 行为 |
| --- | --- | --- |
| `__init__` | 82 | 计算 `max_webnum`，按配置创建浏览器 |
| `_normalize_driver` | 91 | Edge/Firefox 的创建函数可能返回 `(driver, path)` 元组，这里统一取 driver |
| `_create_browser_from_config` | 97 | `browser` 配置 → 创建函数映射，非法值抛 `ValueError` |
| `__del__` | 112 | 尽力 `browser.quit()`（依赖 GC 时机，不可靠） |
| `process_request` | 117 | 计数限流、打开页面、滚动、截图、返回 `HtmlResponse` |
| `process_response` | 152 | 原样返回（通常不会走到，因为 `process_request` 已短路） |

浏览器创建函数的返回值差异是这里最容易踩的坑：`create_chrome_driver()` 永远返回 driver；`create_edge_driver()` / `create_firefox_driver()` 在传入 `task_name`+`formatted_time` 且 `enable_ssl_key_log=True` 时返回 `(driver, ssl_key_log_path)`。中间件在 `middlewares.py:110` 统一做了归一化。

截图路径（`middlewares.py:137-143`）：

```text
data/screenshot/<request.url 去掉协议>/<pcap 文件名>.png
例如：data/screenshot/baidu.com/direct_20260320170144_..._baidu.com.pcap.png
```

`spider.pcap_path` 由 `Spider.__init__` 注入，因此截图名与 pcap 名一一对应。

### 5.8 `spider/settings.py` — Scrapy 配置（121 行）

| 设置 | 值 | 说明 |
| --- | --- | --- |
| `SPIDER_MODULES` | `spider_traffic.spider.spiders` | 由 `src/scrapy.cfg` 指向本 settings |
| `USER_AGENT` | Windows Chrome/Edge 131 UA | 固定值 |
| `ROBOTSTXT_OBEY` | `False` | 不遵守 robots |
| `CONCURRENT_REQUESTS` | 16 | 实际被中间件短路，意义有限 |
| `DOWNLOAD_DELAY` | `config["spider"]["download_delay"]`（当前 60） | 导入时读取 |
| `RANDOMIZE_DOWNLOAD_DELAY` | `True` | |
| `LOG_LEVEL` | `INFO` | |
| `LOG_FILE` | `logs/scrapy_<YYYY>_<M>_<D>.log` | 月/日不补零 |
| `DOWNLOADER_MIDDLEWARES` | `SpiderDownloaderMiddleware: 543` | 唯一自定义中间件 |
| `TWISTED_REACTOR` | asyncio reactor | |
| `FEED_EXPORT_ENCODING` | `utf-8` | |

`ITEM_PIPELINES`、`SPIDER_MIDDLEWARES`、`AUTOTHROTTLE`、`HTTPCACHE` 均为注释状态。

### 5.9 浏览器适配层

三个模块共享一套约定：按 `SPIDER_MODE` 决定代理、headless 运行、参数降噪、提供进程清理函数，并统一由 `spider/__init__.py:13 kill_browsers()` 分发。

#### 5.9.1 `chrome.py`（159 行）

- `generate_normal_random(mean=5, variance=10)`：`random.gauss(5, √10)`，绝对值 ≤2 时取 2，再取绝对值；用于滚动等待。
- `create_chrome_driver()`（28 行）：
  - 代理：xray → `--proxy-server=http://host:port`；tor → `socks5://host:port`；
  - `disable_quic=true` → `--disable-quic`；
  - `--headless=new`、`--no-sandbox`、`--incognito`、`--disable-dev-shm-usage` 等；
  - 下载目录 `data/download`；
  - `set_capability("goog:loggingPrefs", {"performance": "ALL"})`（性能日志能力，代码未消费）；
  - 通过 CDP 注入脚本把 `navigator.webdriver` 置为 `undefined`；
  - 使用 `webdriver.Chrome(options=...)`，依赖 PATH/Selenium Manager 找到 chromedriver。
- `scroll_to_bottom(driver)`（90 行）：循环滚动到底部，等待高度增长；每次延时 `generate_normal_random()/times`；达到 `scroll_num` 或高度不再变化则停止。
- `add_cookies(browser)`（129 行）：读取 `youtube_cookie.txt`，**当前无人调用**。
- `kill_chrome_processes()`（137 行）：`pkill -f chromedriver`、`pkill -f google-chrome`。

> 注意：`scroll_to_bottom` 定义在 `chrome.py`，但被 `middlewares.py:17` 导入并**用于所有浏览器**。函数内部只使用通用 JS，因此功能正常，但模块归属容易误导。

#### 5.9.2 `edge.py`（251 行）

- 可执行文件解析：`_resolve_from_candidates()` 按 **环境变量 → PATH → 常见路径** 顺序查找；
  - driver 环境变量：`MSEDGEDRIVER` / `EDGE_DRIVER` / `EDGEWEBDRIVER`；
  - 浏览器环境变量：`MSEDGE_BINARY` / `EDGE_BINARY`；
  - 常见路径包含 `/usr/bin/microsoft-edge`、`/opt/microsoft/msedge/msedge` 等。
- 参数比 Chrome 更激进地降噪：禁用 AsyncDns、Translate、MediaRouter、OptimizationHints 等 8 项 feature，`--no-pings`、`--disable-sync`、`--dns-prefetch-disable`、`--log-net-log=/tmp/netlog.json --net-log-capture-mode=Everything` 等。
- `--ssl-key-log-file=<path>` 只在显式传入 `task_name+formatted_time` 时添加；中间件调用时未传，因此实际依赖 `SSLKEYLOGFILE` 环境变量。
- 通过 CDP 设置 `Accept-Language` 并把 `navigator.webdriver/language/languages` 覆写为中文环境。
- `kill_edge_processes()`（235 行）用 `pkill -KILL -f` 匹配 `msedgedriver`、`microsoft-edge`、`microsoft-edge-stable`、`msedge`。

> 安全/体积提示：`--log-net-log=/tmp/netlog.json` 会记录完整网络事件日志且无大小上限，每次运行都会覆盖写入 `/tmp`。

#### 5.9.3 `firefox.py`（331 行）

- `kill_firefox_processes()`（72 行）用 `pkill -KILL -x`（精确匹配）处理 `geckodriver`/`firefox-esr`/`firefox`。
- `_resolve_firefox_binary()` 优先真实 ELF 二进制路径（`/snap/firefox/current/usr/lib/firefox/firefox`、`/usr/lib/firefox/firefox` 等），避免 geckodriver 拒绝 `/usr/bin/firefox` 启动脚本。
- `create_firefox_driver()`（147 行）：
  - `TLSSECRETS`：若显式传入 `task_name` 则设置 `os.environ["SSLKEYLOGFILE"]=<ssl_key_dir>/<prefix><time>_<task>_ssl_key.log`；中间件未传参，故沿用主进程的 `/tmp/sslkeys.log`；
  - `-headless -private`；
  - `disable_quic=true` → 关闭 `network.http.http3.enabled` 与 `network.http.altsvc.enabled`；
  - `network.trr.mode=5` 禁 DoH，`security.OCSP.enabled=0`；
  - 代理：xray → `network.proxy.type=1` + http/https 代理；tor → socks5 + `socks_remote_dns=true`；
  - 大量降噪设置：遥测、Normandy、预取/预连接、Remote Settings、扩展更新、GMP 更新、新标签页推荐、SafeBrowsing、搜索建议、定位、下载行为等（232-319 行）；
  - geckodriver 必须存在，否则抛 `FileNotFoundError`。
- 文件头部的 `JS_SELECT_ALL_AND_COPY_CAPTURE` 大段 JS 常量（17-69 行）**没有任何引用**，属于遗留实验代码。

#### 5.9.4 `spider/__init__.py`

`BROWSER_CLEANERS` 把 `edge/chrome/firefox` 映射到各自的清理函数，`kill_browsers(browser_type)` 查表调用；未知类型仅打印警告（`print`，不走 logger）。

### 5.10 `spider/items.py` / `pipelines.py`

两者都是 Scrapy 模板空壳（12 行 / 13 行），未启用。项目不产出 Scrapy item，数据全部以文件形式落盘。

### 5.11 `tls_decoder/flow_key.py` — 方向无关流键（32 行）

把 `(src_ip, src_port)` 与 `(dst_ip, dst_port)` 排序，构造规范化端点对，重写 `__hash__`/`__eq__`/`__repr__`。因此 `A→B` 与 `B→A` 的包会归入同一个 `FlowKey`，`__repr__` 形如：

```text
Flow(130.126.157.20:443 <-> 192.168.77.8:57760)
```

这个字符串就是解码 JSON 的顶层键。

### 5.12 `tls_decoder/http2decoder.py` — TLS 解码器（355 行）

类 `TLSStreamDecoder(pcap_file, sslkeylog_file, logger=logger)`，内部结构：

```python
self.connections: Dict[FlowKey, {
    "sni": str|None,
    "streams": Dict[stream_id, stream_dict],
    "client_ip": str,
    "http1_unmatched_req_count": int,
}]
```

#### 5.12.1 解码主循环（`decode()`，27-50 行）

```python
pyshark.FileCapture(
    pcap_file,
    display_filter="tls.handshake || http2 || http",
    override_prefs={"tls.keylog_file": sslkeylog_file},
    keep_packets=False,
)
```

- `tls.handshake` 用于提取 SNI 并建立流表；
- `http2` 与 `http` 只有在提供密钥、tshark 成功解密后才会出现；
- `keep_packets=False` 降低内存占用；
- 单包解析异常只记 warning，不中断整体解码。

#### 5.12.2 包处理（`_process_packet`，52-84 行）

1. 用 IP/IPv6 + TCP 端口构造 `FlowKey`；
2. 首次见到该流时记录 `client_ip`（首个包的源 IP）作为方向基准；
3. `_get_sni()` 读 `tls.handshake_extensions_server_name`；
4. 若包内含 `HTTP2` 层 → `_process_http2_packet()`；否则若含 `HTTP` 层 → `_process_http1_packet()`。

#### 5.12.3 HTTP/1.1 重建（87-142 行）

- 请求判定：`hasattr(http, "request_method")`；
- 每个请求分配伪流 ID `http1-<递增计数>`；
- 记录 `host`、`request_uri`、`request_time`、包号；`content_length` 作为请求体大小；
- 响应匹配：从最新请求向前找第一个 `status is None` 的流（**LIFO 近似**，无严格事务 ID）；
- 响应字段：`response_code`、`content_type`、`file_data`（重组后的响应体十六进制），`resource_data_size = len(bytes.fromhex(file_data))`；
- HTTP/1.1 的 `response_start_time == response_end_time == 该响应包时间`，所以 **`duration_sec == ttfb_sec`**（见实测样例）。

#### 5.12.4 HTTP/2 重建（145-277 行）

- 只处理 `type == "1"`（HEADERS）与 `type == "0"`（DATA）；
- HEADERS：`headers_path` 存在视为请求（记录 `request_time`/`headers_packet_num`）；`headers_authority` → `domain`（缺失时回退 SNI）；`headers_status` → 状态码；`headers_content_type` → MIME；
- DATA：`http2.length` 累加到请求或响应大小（依据 `pkt` 源 IP 与 `client_ip` 是否相同），并记录包号；首个响应 DATA 的时间作为 `response_start_time`，最后一个作为 `response_end_time`；
- 不处理 CONTINUATION、PUSH_PROMISE、RST_STREAM、优先级帧，也不处理填充字节。

#### 5.12.5 结果汇总（`get_results()`，279-338 行）

- 过滤条件：`response_packet_nums` 非空且 `status` 非空，否则该流被丢弃（**但流所属的 Flow 仍会出现在结果里，只是 `resources` 为空**）；
- URL 拼接：`https://{domain}{path}`，缺失 path 时为 `https://{domain}/`，都缺失则为 `"incomplete_url"`；
- 指标：
  - `duration_sec = response_end_time - request_time`
  - `ttfb_sec = response_start_time - request_time`
- 每个 Flow 的 resources 按 `request_time` 升序（无法比较的排最后）；
- 顶层返回 `{ "<FlowKey repr>": {"sni": ..., "resources": [...] } }`。

#### 5.12.6 独立运行

`if __name__ == "__main__"`（341-355 行）用相对路径 `../test/test.pcap`、`../test/sslkeys.log` 解码并写 `../test/results.json`，需要从 `src/` 目录运行。

### 5.13 `tls_decoder/TrojanDecoder.py` — Trojan 双层解码（261 行）

`class TrojanDecoder(pcap_path, out_keylog_path, tls_keylog, json_path)`，入口函数：

```python
action(pcap_path, keylog_path, tls_keylog, json_path)   # TrojanDecoder.py:238
```

流程（`run()` → `_out_decoder_pcap()` → `save_results_to_json()`）：

1. **外层剥离**：循环 `i` 从 0 到 999998，执行
   `tshark -r <pcap> -o tls.keylog_file:<xray 外层密钥> -q -z follow,tls,raw,<i>`；
   - 用 `pyshark.tshark.tshark.get_process_path()` 定位 tshark；
   - stdout 中出现 `"Node 0: :0"` 视为流已耗尽，跳出循环；
   - 否则把该流的原始十六进制 dump 写入 `/tmp/raw_hex.txt`。
2. **重建内层 pcap**（`_create_pcap_from_hex_file()`，35-161 行）：
   - `_parse_stream_info()` 用正则 `Node\s+(\d+):\s+([\d.]+):(\d+)` 解析两端地址；
   - 依据**缩进**判断方向（缩进行 = 客户端 → 服务端）；
   - 用 `scapy` 构造随机 ISN 的 SYN / SYN-ACK / ACK，再按行生成 `PA` 数据包，`Raw(load=payload)`；
   - 首个数据包会尝试把 `160301` 之前的前缀裁掉（对齐 TLS Handshake 记录头）；
   - 输出到 `/tmp/decoded_tls.pcap`（`self.decoded_pcap_path` 会被赋为绝对路径，`os.path.join` 结果即该绝对路径）。
3. **内层解码**：用浏览器密钥 `<pcap>.log` 实例化 `TLSStreamDecoder`，对重建的 pcap 再跑一遍，`self.trace_results.update(results)` 合并多流结果。
4. **落盘**：把合并后的字典写 `<pcap>.json`。

局限：强依赖 tshark 的输出格式（`follow,tls,raw` 的 Node 行格式与缩进语义），任何 tshark 版本变化都可能破坏解析；重建包的时间戳为构造时的当前时间（scapy 默认），因此**内层 JSON 的 `duration_sec`/`ttfb_sec` 不具备真实网络时序意义**。

### 5.14 `myutils/`

| 文件 | 内容 |
| --- | --- |
| `__init__.py` | `project_path`（3 行） |
| `config.py` | 模块级 `configparser.ConfigParser`，导入时读取 `config/config.ini`；导出 `SPIDER_MODE`；无异常兜底 |
| `logger.py` | `setup_logging(filename)`：`RotatingFileHandler(maxBytes=50MB, backupCount=3)` + 控制台 handler（INFO），格式 `%(asctime)s - %(levelname)s - %(message)s`；模块级导出 `logger`（defult.log）与 `logger_url`（url.log） |

注意：`setup_logging` 的注释写的是 500MB，实际代码是 50MB；logger 未设置 `propagate=False`，若上层另行配置 root logger 可能出现重复输出；每个 logger 各自持有一个 `RotatingFileHandler`，因此 `logs/` 目录必须存在且可写。

---

## 6. 配置参考

### 6.1 `config/config.ini`

程序只读取以下键（已用全量代码扫描确认）；未列出的键不会被任何代码使用。

| Section | 键 | 必需 | 类型 | 当前示例 | 作用与读取位置 |
| --- | --- | --- | --- | --- | --- |
| `information` | `name` | 是 | str | `<node-ip>` | pcap 文件名中的节点标识（`main.py:170`） |
| `information` | `site` | 是 | str | `us` | pcap 文件名中的位置标识（`main.py:171`） |
| `information` | `protocal` | xray/tor 必需 | str | `https` | 文件名前缀与 trojan 解码判断（`main.py:188,192`）；**拼写为 `protocal`** |
| `information` | `ip_addr` | xray 必需 | str | `<node-ip>` | xray 抓包过滤 `host <ip_addr>`（`capture.py:16`） |
| `proxy` | `host` | 是 | str | `127.0.0.1` | 浏览器代理主机（三个浏览器模块） |
| `proxy` | `port` | 是 | int | `10809` | 浏览器代理端口（三个浏览器模块） |
| `spider` | `mode` | 是 | enum | `direct` | `direct`/`xray`/`tor`，导入期读取（`config.py:13`） |
| `spider` | `browser` | 否（默认 chrome） | enum | `firefox` | `chrome`/`edge`/`firefox`（`main.py:173`、`middlewares.py:98`） |
| `spider` | `time_per_website` | 是 | int（秒） | `30` | 单站爬取时限（`action.py:37`） |
| `spider` | `download_delay` | 是 | int（秒） | `60` | Scrapy `DOWNLOAD_DELAY`（`settings.py:35`） |
| `spider` | `scroll` | 是 | bool 字符串 | `true` | 是否滚动页面（`middlewares.py:133`） |
| `spider` | `scroll_num` | 是 | int | `3` | 最大滚动次数（`chrome.py:119`） |
| `spider` | `webnum` | 是 | int | `1` | 每站最大请求数，`-1` 表示不限（`middlewares.py:85`） |
| `spider` | `disable_quic` | 否（默认 false） | bool 字符串 | `true` | 关闭 QUIC/HTTP3（三浏览器） |
| `spider` | `is_decode` | 是 | bool 字符串 | `false` | 是否在采集后解码（`main.py:172,278`） |

**无效/遗留键**：`depth`、`multisite_num` 在当前代码中无任何引用，可删除。
**大小写**：布尔值通过 `.lower() == "true"` / `.strip().lower() == "true"` 判断，`True`/`TRUE` 均可。

> 注意 `chrome.py:48` 直接索引 `config["spider"]["disable_quic"]`（无默认值），而 `edge.py`/`firefox.py` 用 `.get(..., "false")`。若删除该键，Chrome 会 `KeyError`。

### 6.2 URL 列表：`config/current_docker_url_list.txt`

- 每行一个**主机名**，不是完整 URL；
- 支持空行与 `#` 注释行（`task.py:43-48`）；
- 程序内部拼 `https://`（`task.py:53`）；
- 该文件同时决定 `allowed_domains`（`task.py:56`），影响站内爬取范围；
- 当前内容为 `baidu.com / douban.com / zhihu.com / bilibili.com`。

另外两个文件 `current_docker_url_list_background.txt`（9419 行）与 `current_docker_url_list_monitor.txt`（99 行）是 `multi-site` 分支的遗留资产，**当前主分支没有任何代码引用**。

### 6.3 链接过滤：`config/exclude_keywords`

- 一行一个关键字，`trace.py:34` 做子串匹配；
- 当前内容：`egister/ignin/ogin/Help/help/policies/Policies/policy/Policy/account`；
- **缺陷**：`task.py:37` 把 `\n` 替换为空格，匹配串带尾随空格，导致 `https://site/register?x=1` 这类链接**不会被过滤**（关键字后紧跟的是 `?` 而非空格）。修复方式：改为 `s.strip()`。

### 6.4 任务下标：`config/running.json`

```json
{"currentIndex": 0}
```

- 首次启动不存在时自动创建为 0（`task.py:26-30`）；
- 每完成一站由 `main.py:285-287` 回写；
- 手工修改可实现"从第 N 个 URL 开始"。

### 6.5 Xray 配置

`main.py:190` 固定加载 `config/xray.json`：

```python
config_path = os.path.join(project_path, "config", "xray.json")
```

仓库中提供的是 `xray_trojan.json` 与 `xray_ss.json`，**`xray.json` 本身不存在**（且被 `.gitignore` 单独忽略）。使用 xray 模式前必须自行创建，例如：

```bash
cp config/xray_trojan.json config/xray.json
```

关键字段（以 `config/xray_trojan.json` 为例）：

| 字段 | 值 | 与本项目的关系 |
| --- | --- | --- |
| `inbounds[].protocol` | `socks` / `http` | 浏览器使用 http 代理（`spider/*.py`） |
| `inbounds[].port` | socks `10801` / http `10800` | **必须与 `config.ini` 的 `[proxy] port` 一致**，当前配置为 `10809`，不一致 |
| `outbounds[].streamSettings.tlsSettings.masterKeyLog` | `/tmp/xray_sslkeylog.log` | 与 `main.py:28` 期望的路径一致，是 `.xray.log` 的来源 |
| `log.loglevel` | `warning` | Xray 自身日志级别 |

`config/xray_ss.json` **没有 `masterKeyLog`**，因此 SS 出口无法产出外层密钥，`_decode_xray_mode()` 即使被调用也无法真正解码。

> 配置文件包含真实服务器地址与密码，属于敏感资产；请在分享、截图、提交前脱敏。

### 6.6 Tor 配置：`config/torrc`

`torDo.py:45` 固定读取 `config/torrc`，仓库中**不存在该文件**，需要自行编写（SocksPort 需与 `config.ini` 的 `[proxy] port` 对齐）。日志输出到 `logs/notice.log`。

### 6.7 环境变量

| 变量 | 设置位置 | 作用 |
| --- | --- | --- |
| `SSLKEYLOGFILE` | `main.py:169` 设为 `/tmp/sslkeys.log` | 浏览器导出 TLS 会话密钥；`firefox.py:192` 在显式传参时会覆盖 |
| `SE_OFFLINE` | `edge.py:116`、`firefox.py:189` 设为 `true` | 抑制 Selenium Manager 联网下载 driver |
| `MSEDGEDRIVER` / `EDGE_DRIVER` / `EDGEWEBDRIVER` | `edge.py:46` 读取 | 指定 EdgeDriver |
| `MSEDGE_BINARY` / `EDGE_BINARY` | `edge.py:58` 读取 | 指定 Edge 二进制 |
| `FIREFOX_BIN` / `FIREFOX_BINARY` | `firefox.py:121` 读取 | 指定 Firefox 二进制 |
| `GECKODRIVER` / `WEBDRIVER_GECKO_DRIVER` | `firefox.py:138` 读取 | 指定 geckodriver |

---

## 7. 数据产物与格式

### 7.1 产物总览

| 产物 | 路径模板 | 生成条件 |
| --- | --- | --- |
| 原始抓包 | `data/pcap/<host>/<PROTOCAL>_<YYYYMMDDHHMMSS>_<name>_<site>_<host>.pcap` | 每次采集 |
| 浏览器 TLS 密钥 | `<pcap 去扩展名>.log` | 浏览器写 `/tmp/sslkeys.log` 且归档时存在 |
| Xray 外层密钥 | `<pcap 去扩展名>.xray.log` | xray 模式且 `/tmp/xray_sslkeylog.log` 存在 |
| 解码结果 | `<pcap 去扩展名>.json` | `is_decode=true` 且解码成功 |
| 页面截图 | `data/screenshot/<host+path>/<pcap 文件名>.png` | 每次实际发出的请求 |
| 主程序日志 | `logs/defult.log`（滚动 50MB × 3） | 常驻 |
| URL 访问日志 | `logs/url.log` | 每次请求 |
| Scrapy 日志 | `logs/scrapy_<Y>_<M>_<D>.log` | 每进程 |
| 下载文件 | `data/download/`（Chrome）、`<cwd>/download`（Edge/Firefox） | 页面触发下载时 |
| Tor 日志 | `logs/notice.log` | tor 模式 |

注意：`data/screenshot/`、`data/download/` 在当前 `data/` 中并不存在（历史运行以 `is_decode=false`、`webnum=1` 为主，且产物以 pcap 为唯一留存）。当前 `data/pcap/` 下共有 2925 个 pcap（`baidu.com`、`bilibili.com`、`douban.com`、`zhihu.com` 四个目录），全部由 `root` 生成。

### 7.2 pcap 文件名与示例

```text
data/pcap/baidu.com/direct_20260320170144_<node-ip>_us_baidu.com.pcap
              └ host ┘ └协议┘ └── 时间戳 ──┘ └── name ──┘ └site┘ └── host ──┘
```

字段依次为 `PROTOCAL_NAME`、`%Y%m%d%H%M%S`、`information.name`、`information.site`、URL 去协议部分（`/` 替换为 `_`）。时间戳精度为秒，同一秒内的重复采集会互相覆盖。

### 7.3 TLS 密钥日志格式

标准 NSS/Chromium keylog 文本：

```text
# TLS secrets log file, generated by OpenSSL / Python
CLIENT_HANDSHAKE_TRAFFIC_SECRET <client_random_hex> <secret_hex>
SERVER_HANDSHAKE_TRAFFIC_SECRET <client_random_hex> <secret_hex>
CLIENT_TRAFFIC_SECRET_0 <client_random_hex> <secret_hex>
SERVER_TRAFFIC_SECRET_0 <client_random_hex> <secret_hex>
```

`test/sslkeys.log` 就是该格式（首行注释来自 OpenSSL）。

### 7.4 解码 JSON Schema

顶层为对象，键是 `FlowKey` 的字符串表示：

```json
{
  "Flow(<ip_a>:<port_a> <-> <ip_b>:<port_b>)": {
    "sni": "<string|unknown>",
    "resources": [
      {
        "stream_id": "<string>",
        "url": "<string>",
        "status": "<string|null>",
        "content_type": "<string|null>",
        "request_data_size": 0,
        "resource_data_size": 0,
        "headers_packet_num": "<int|null>",
        "request_packet_nums": [0],
        "response_packet_nums": [0],
        "request_time": 0.0,
        "response_start_time": 0.0,
        "response_end_time": 0.0,
        "duration_sec": 0.0,
        "ttfb_sec": 0.0
      }
    ]
  }
}
```

字段来源与语义：

| 字段 | 来源 | 说明 |
| --- | --- | --- |
| 顶层键 | `FlowKey.__repr__` | 端点排序后 `Flow(a:p <-> b:p)`，方向无关 |
| `sni` | `tls.handshake_extensions_server_name` | 缺失时为 `"unknown"` |
| `stream_id` | HTTP/2 `http2.streamid` 或 `http1-<n>` | 伪流 ID 仅在本文件内有意义 |
| `url` | `headers_authority`+`headers_path` 或 `http.host`+`http.request_uri` | 统一拼成 `https://`；不完整时字面量 `"incomplete_url"` |
| `status` | `headers_status` / `response_code` | tshark 输出为字符串，如 `"200"` |
| `content_type` | `headers_content_type` / `content_type` | |
| `request_data_size` | HTTP/2 DATA 长度累计 / `http.content_length` | HTTP/1.1 无 body 时为 0 |
| `resource_data_size` | HTTP/2 DATA 长度累计 / `http.file_data` 长度 | 解密后（未解压）的字节数 |
| `*_packet_nums` | `frame.number` | 可用于在 Wireshark 中跳转 |
| `request_time` | 请求帧/包的 `frame.time_epoch` | |
| `response_start_time` / `response_end_time` | 首个/末个响应 DATA 或响应包时间 | HTTP/1.1 两者相等 |
| `duration_sec` | `response_end_time - request_time` | 端到端耗时 |
| `ttfb_sec` | `response_start_time - request_time` | 首字节耗时 |

真实样例（取自 `test/results.json`）：

```json
"Flow(130.126.157.20:443 <-> 192.168.77.8:57760)": {
    "sni": "illinois.edu",
    "resources": [
        {
            "stream_id": "http1-0",
            "url": "https://illinois.edu/",
            "status": "200",
            "content_type": "text/html; charset=UTF-8",
            "request_data_size": 0,
            "resource_data_size": 60768,
            "headers_packet_num": 70082,
            "request_packet_nums": [70082],
            "response_packet_nums": [70482],
            "request_time": 1757486672.502325,
            "response_start_time": 1757486673.144609,
            "response_end_time": 1757486673.144609,
            "duration_sec": 0.6422839164733887,
            "ttfb_sec": 0.6422839164733887
        }
    ]
}
```

### 7.5 日志格式与样例

`logs/defult.log`、`logs/url.log` 格式由 `logger.py:30` 决定：

```text
%(asctime)s - %(levelname)s - %(message)s
```

真实片段（`logs/defult.log`）：

```text
2026-03-20 19:02:17,602 - INFO - 流量采集开始，pcap输出路径 /home/.../data/pcap/zhihu.com/direct_20260320190216_<node-ip>_us_zhihu.com.pcap
2026-03-20 19:02:20,888 - INFO - 关闭浏览器进程
2026-03-20 19:02:50,944 - INFO - 等待流量结束
2026-03-20 19:02:50,976 - INFO - 优雅的关闭流量收集进程
2026-03-20 19:02:50,976 - WARNING - SSLKEYLOGFILE未找到或不存在: /tmp/sslkeys_208720_1773895611.log
2026-03-20 19:02:50,977 - INFO - 第2个url爬取完成，爬取下一个url
```

`logs/url.log` 记录"当前起始站 : 实际访问 URL"：

```text
2026-03-18 21:01:03,850 - INFO - https://baidu.com : https://baidu.com
```

> 上述历史日志中出现的 `/tmp/sslkeys_208720_1773895611.log` 说明当时运行的 `main.py` 版本使用了带 PID 的临时文件名，与当前代码的 `/tmp/sslkeys.log` 不同——分析历史日志时需注意版本差异。

---

## 8. 部署与运行

### 8.1 本地环境要求

| 项 | 要求 |
| --- | --- |
| 操作系统 | Linux（浏览器、tcpdump、进程管理均按 Linux 编写；`pkill`/`/tmp` 强依赖） |
| Python | 3.12（当前环境 3.12.3；代码使用 f-string、`Queue`、`multiprocessing` 等通用特性） |
| 系统工具 | `tcpdump`、`tshark`（pyshark 依赖，需在 PATH 中） |
| 浏览器 | Chrome/Edge/Firefox 及其 driver（`bin/` 提供 .deb 与 driver） |
| 权限 | 抓包需要 root 或 `setcap cap_net_raw,cap_net_admin+eip $(which tcpdump)` |
| 目录 | `logs/`、`data/` 必须可写；`config/` 需准备好 6.1 节所列文件 |

### 8.2 权限配置

当前主机实测 `tcpdump` 无 capabilities（`getcap` 为空），非 root 运行会静默产生空 pcap。两种方案：

```bash
# 方案 A：一次性授权（推荐）
sudo setcap cap_net_raw,cap_net_admin+eip "$(which tcpdump)"

# 方案 B：以 root 运行
sudo -E .venv/bin/python3 -m spider_traffic.main     # 注意 -E 保留 SSLKEYLOGFILE
```

若以 root 运行过一次，`logs/defult.log` 会变为 root 所有，之后普通用户运行将因无法打开该文件而在导入期直接 `PermissionError`（本次验证即遇到，见 [第 10 章](#10-验证记录)）。处理：`sudo chown -R "$USER" logs data`。

### 8.3 本地快速开始

```bash
# 1) 虚拟环境
python -m venv .venv
. .venv/bin/activate
pip install -r requirements.txt

# 2) 准备配置（示例）
cat > config/config.ini <<'INI'
[information]
name = local_node
protocal = direct
site = lab
ip_addr = 127.0.0.1

[proxy]
host = 127.0.0.1
port = 10809

[spider]
browser = firefox
time_per_website = 30
download_delay = 60
mode = direct
scroll = true
scroll_num = 3
webnum = 1
disable_quic = true
is_decode = true
INI

printf 'baidu.com\nzhihu.com\n' > config/current_docker_url_list.txt
printf 'ogin\negister\naccount\n' > config/exclude_keywords
echo '{"currentIndex": 0}' > config/running.json
mkdir -p logs data

# 3) 运行
./action.sh
# 等价于： cd src && ../.venv/bin/python3 -m spider_traffic.main
```

启动脚本 `action.sh` 内容极简：

```bash
#!/bin/bash
cd ./src
../.venv/bin/python3 -m spider_traffic.main
```

因此**当前工作目录必须是仓库根**，且 `.venv` 必须位于仓库根（`main.py:162` 依赖 `../.venv/bin/python3`）。

### 8.4 Docker

`Dockerfile` 步骤：

1. `FROM <private-registry>/ubuntu:latest`（内网私有镜像，**外部环境需替换为基础镜像**；实际地址见 `Dockerfile:1`，此处按安全规范脱敏）；
2. 安装 GUI/字体依赖（`libnss3`、`libgbm1`、`libgtk-3-0` 等）；
3. 换 aliyun 源，安装 `python3.12`、`python3.12-venv`、`tcpdump`、`tshark`、`libpcap-dev`、`ethtool`、编译工具等；
4. `ADD . .` → `python -m venv .venv` → 安装 `requirements.txt`；
5. 依次 `dpkg -i` 安装 Chrome / Edge / Firefox 的 .deb，并把 `chromedriver`、`msedgedriver`、`geckodriver` 复制到 `/usr/bin`；
6. `CMD ["bash"]`。

构建与运行（README 推荐）：

```bash
docker build -t aimafan/spider_traffic:v1 .

docker run --privileged --network host \
  -v $(pwd)/config:/app/config \
  -v $(pwd)/data:/app/data \
  -v $(pwd)/logs:/app/logs \
  aimafan/spider_traffic:v1 \
  bash -lc "cd /app/src && ../.venv/bin/python3 -m spider_traffic.main"
```

要点：

- `--privileged` 用于 tcpdump；`--network host` 让抓包与本地代理端口直通；
- `--network host` 下浏览器与代理共享宿主网络栈，无需额外端口映射；
- 镜像内 Chrome/Edge/Firefox 版本需与 `bin/` 中 driver 版本匹配，否则 Selenium 会报 session 创建失败；
- Dockerfile 注释提到 `ethtool -K eth0 tso off gso off gro off` 需要在**容器启动后**执行（用于消除网卡合并对时间测量的影响），默认未执行：
  ```bash
  docker run ... bash -lc "sudo ethtool -K eth0 tso off gso off gro off && ./action.sh"
  ```

`.dockerignore` 排除 `.venv`、`test`、`*.tar`、`data/*`、`logs/*`，但**保留 `bin/`**（构建必需）。

### 8.5 多平台 Dockerfile 变体

| 文件 | 基础镜像 | Python | 差异 |
| --- | --- | --- | --- |
| `Dockerfile` | ubuntu:latest（私有仓库） | 3.12 | 主线，安装三浏览器 + 三 driver |
| `dockerfiles/Dockerfile_ubuntu24` | ubuntu:latest | 3.12 | 与主线接近，`libasound2t64`，仅安装 Chrome |
| `dockerfiles/Dockerfile_ubuntu20` | ubuntu:20.04 | 通过 deadsnakes PPA | `libasound2`，仅安装 Chrome；需替换 `requirements/requirements_ubuntu20.txt` |
| `dockerfiles/Dockerfile_debian12` | debian:latest | 系统 python3 | 使用 `debian.sources`，仅安装 Chrome |

三个变体都只安装 Chrome，需要 Edge/Firefox 时应以主线 `Dockerfile` 为准。

### 8.6 `bin/` 二进制清单

| 路径 | 大小 | 版本/说明 |
| --- | --- | --- |
| `bin/Xray-linux-64/xray` | 44M | Xray 24.11.11（go1.23.3） |
| `bin/Xray-linux-64.tar` | 44M | Xray 压缩包（冗余） |
| `bin/tor_s/tor` | 51M（目录） | Tor 0.4.7.13 |
| `bin/tor_s/{obfs4proxy,meek-client,snowflake-client,webtunnelClient}` | — | 各类桥接插件（代码未使用） |
| `bin/chromedriver-linux64/chromedriver` | 17M | ChromeDriver |
| `bin/google-chrome-stable_current_amd64.deb` | 103M | Chrome 安装包 |
| `bin/msedgedriver` | 19M | EdgeDriver |
| `bin/microsoft-edge-stable_146.0.3856.62-1_amd64.deb` | 180M | Edge 安装包 |
| `bin/geckodriver` | 5.9M | GeckoDriver |
| `bin/firefox_148.0.2+build1-0ubuntu0.24.04.1~mt1_amd64.deb` | 79M | Firefox 安装包 |

---

## 9. 流量解码原理

### 9.1 为什么能解密

浏览器在启动时通过 `SSLKEYLOGFILE` 环境变量导出 TLS 1.2/1.3 的会话密钥（`CLIENT_RANDOM` / `*_TRAFFIC_SECRET_*`）。`pyshark.FileCapture(..., override_prefs={"tls.keylog_file": path})` 让 tshark 用这些密钥解密，并进一步交给 `http` / `http2` 解析器重组应用层。

必要前提：

1. 浏览器确实写出了 keylog（headless + `SSLKEYLOGFILE` 通常有效）；
2. 抓包覆盖了密钥对应的连接（xray 模式只抓 `host ip_addr`，内层连接在被解密的外层隧道里，靠 tshark 无法直接看到；所以 xray 需要额外的"外层剥离"步骤）；
3. 未使用 ECH / 未走 QUIC（否则 tshark 无法解密）；
4. keylog 文件与 pcap 一一对应（本项目通过"每站移动 keylog 文件"实现）。

### 9.2 direct 模式：HTTP/1.1 与 HTTP/2 重建

1. tshark 按 `tls.handshake || http2 || http` 过滤；
2. 每个 TCP 流建立 `FlowKey` 条目，SNI 来自 ClientHello；
3. HTTP/2：按 stream id 聚合成资源，HEADERS 提供 URL/状态/MIME，DATA 累加大小并给出响应时序；
4. HTTP/1.1：按"最近的未响应请求"做 LIFO 匹配，响应体大小取 `file_data` 长度；
5. 丢弃无响应或无状态码的流，输出 JSON。

### 9.3 指标语义与偏差

| 指标 | 计算 | 偏差来源 |
| --- | --- | --- |
| `ttfb_sec` | 首个响应 DATA/响应包 − 请求帧时间 | 不含 DNS、TCP、TLS 握手时间；HTTP/2 下服务端可能先发 HEADERS 再发 DATA，HEADERS 时间不计入 |
| `duration_sec` | 末个响应 DATA/响应包 − 请求帧时间 | 未包含客户端解析时间；HTTP/1.1 因单包重组导致 `duration == ttfb` |
| `resource_data_size` | DATA 帧 payload 长度之和 / `file_data` 字节数 | 为压缩后（Content-Encoding 未解）体积，且 HTTP/2 不含帧头与填充 |

### 9.4 xray(trojan) 模式：外层剥离 + 内层重建

```text
pcap（含 TCP 到代理服务器的外层 TLS）
  │ tshark -z follow,tls,raw,<i>   （用 /tmp/xray_sslkeylog.log 解外层）
  ▼
/tmp/raw_hex.txt（按缩进区分方向的十六进制流）
  │ scapy 构造 SYN/SYN-ACK/ACK + PA 包
  ▼
/tmp/decoded_tls.pcap（重建的内层 TCP/TLS 流）
  │ TLSStreamDecoder（用浏览器 keylog 解内层）
  ▼
<pcap>.json
```

该链路的三个关键假设：tshark 的 `follow,tls,raw` 输出格式稳定；缩进能正确表示方向；重建包的 TCP 序号连续性足以让 tshark 完成 TLS 记录重组。任何一条不成立都会导致内层解码为空。

### 9.5 覆盖范围与限制

| 协议/特性 | 是否支持 | 原因 |
| --- | --- | --- |
| HTTPS over TLS1.2/1.3（TCP） | ✅ | keylog 解密 |
| HTTP/1.1 | ✅ | `http` 解析器 |
| HTTP/2 | ✅ | `http2` 解析器（HEADERS/DATA） |
| HTTP/2 服务端推送 / CONTINUATION | ❌ | 代码未处理对应帧类型 |
| QUIC / HTTP3 | ❌ | 默认禁用；即使启用也无法用 keylog 解密 |
| TLS ECH / 加密 SNI | ❌ | 无法取 SNI |
| 明文 HTTP(80) | ❌ | 抓包阶段显式 `not port 80` |
| WebSocket / gRPC | ❌ | 未解析 |
| 非浏览器流量（系统更新、后台服务） | 部分 | 只要在同网卡且被 keylog 覆盖即可解码，但会污染结果 |

---

## 10. 验证记录

本节记录编写文档时实际执行的验证，供复现。

### 10.1 环境实测

```text
Python      3.12.3（.venv）
tshark      4.4.9
tcpdump     4.99.4 / libpcap 1.10.4
Xray        24.11.11
Tor         0.4.7.13
浏览器       /usr/bin/firefox、/usr/bin/google-chrome、/usr/local/bin/{msedgedriver,geckodriver}
```

### 10.2 全量语法与导入检查

```bash
for f in $(find src -name '*.py'); do .venv/bin/python3 -m py_compile "$f"; done   # 全部通过
# 逐模块 import（在隔离副本中执行，避免污染仓库 logs/）
spider_traffic.main / action / torDo / traffic.capture /
spider.middlewares / spider.chrome / spider.edge / spider.firefox /
spider.task / tls_decoder.TrojanDecoder / myutils.config                  # 全部 OK
```

### 10.3 解码器端到端实测

在源码副本中执行（原仓库因 `logs/defult.log` 属 root 而无法导入，见 10.4）：

```python
from spider_traffic.tls_decoder.http2decoder import TLSStreamDecoder
d = TLSStreamDecoder(pcap_file="test/test.pcap", sslkeylog_file="test/sslkeys.log")
d.decode(); r = d.get_results()
```

结果：

```text
flows: 67        total_resources: 77
Flow(130.126.157.20:443 <-> 192.168.77.8:57760)  sni=illinois.edu
    http1-0  200  https://illinois.edu/   size=60768  ttfb=0.6422839164733887
Flow(104.17.24.14:443 <-> 192.168.77.8:39608)    sni=cdnjs.cloudflare.com
    1  200  https://cdnjs.cloudflare.com/.../webfonts/fa-solid-900...  size=150020 ttfb=0.0475
Flow(192.168.77.8:57432 <-> 203.208.39.194:443)  sni=update.googleapis.com
    1  200  https://update.googleapis.com/service/update2/json?... size=1793
```

与仓库中既有的 `test/results.json`（同 pcap、同 keylog）在流数与资源数上一致（67 / 77），HTTP/1.1 样例的 `duration_sec == ttfb_sec == 0.6422839164733887`，验证了"HTTP/1.1 单包重组导致两指标相等"的结论。同时观察到大量 `resources: []` 的流（如各类 `mon-va.byteoversea.com` 埋点），说明只握手或只有单向数据的连接会被保留为空资源项。

### 10.4 发现的运行环境问题

以当前用户直接导入 `spider_traffic` 会失败：

```text
PermissionError: [Errno 13] Permission denied: '.../logs/defult.log'
```

原因是 `logs/defult.log` 由 root 运行产生，属主为 `root:root`、权限 `644`，普通用户无法以 append 模式打开，而 `logger.py` 在**模块导入期**就创建 handler。修复：

```bash
sudo chown -R "$USER":"$USER" logs data config
```

---

## 11. 已知问题与风险清单

按影响面分级。所有条目均标注证据位置。

### 11.1 高优先级

| # | 问题 | 证据 | 影响 | 建议 |
| --- | --- | --- | --- | --- |
| H1 | `run_action_script` 写死 `../.venv/bin/python3` 且不检查返回码 | `main.py:162-164` | 虚拟环境改名/不存在、或工作目录不对时，子进程静默失败，pcap 里只有空流量，主循环照常继续 | 改为 `sys.executable`，并检查 `returncode` |
| H2 | `xray.json` 与 `torrc` 缺失 | `main.py:190`、`torDo.py:45`；`ls config/` | xray/tor 模式无法直接运行，且 Xray 的 stderr 被丢弃，失败无日志 | 提供模板 + 启动前校验文件存在并明确报错 |
| H3 | 抓包进程 stdout/stderr 被丢弃 | `capture.py:49-54` | 权限不足、过滤语法错误等均不可见，只得到 0 字节 pcap | 至少重定向到日志文件 |
| H4 | `exclude_keywords` 关键字带尾随空格 | `task.py:37` | 登录/注册页过滤几乎失效，爬虫会进入无关页面 | 改为 `s.strip()` 并过滤空串 |
| H5 | `logs/defult.log` 属主问题导致导入失败 | 本次实测（10.4） | 普通用户完全无法启动 | 启动前检查/自愈权限，或在 `setup_logging` 中捕获异常降级到控制台 |
| H6 | 代理端口与 Xray 配置不一致 | `config.ini` port=10809；`xray_trojan.json` 10800/10801 | xray/tor 模式下浏览器代理指向错误端口，全部请求失败 | 统一端口，或从 Xray 配置自动推导 |
| H7 | 解码结果包含非法/敏感明文（密钥、URL、证书信息） | `data/`、`logs/`、`test/` 均含真实流量与密钥 | 误提交即泄露；`config/*` 虽被忽略，但 `*.tar`（5.2GB 备份）也在仓库根 | 明确 `.gitignore` 覆盖，避免在共享环境保留 keylog |
| H8 | Edge 恒定写 `/tmp/netlog.json`（Everything 级别） | `edge.py:178-179` | 敏感网络事件全量落盘、磁盘占用无上限 | 移除或改为按需开启 |

### 11.2 中优先级

| # | 问题 | 证据 | 影响 | 建议 |
| --- | --- | --- | --- | --- |
| M1 | `_save_sslkey_files` 提前 return，导致 xray 密钥不归档 | `main.py:30-34` | xray 模式下 `.xray.log` 丢失，trojan 解码必然失败 | 把 xray 归档移出前置条件 |
| M2 | `_decode_xray_mode` 只在 `protocal == "trojan"` 时解码，其余静默返回 True | `main.py:144-154` | 配置写 `https`（当前值）时永远不解码，且上层认为"成功" | 明确返回 `False` 并告警 |
| M3 | HTTP/1.1 响应匹配是 LIFO 启发式 | `http2decoder.py:121-138` | 并发/流水线场景可能把响应配到错误请求 | 引入按端口/连接的顺序队列或 h2 式流标识 |
| M4 | `stop_capture` 是损坏的死代码 | `capture.py:62-72`（未定义 `process`、不存在的 `config["spider"]["i2pd_path"]`） | 误用会抛异常 | 删除或修复 |
| M5 | `torDo.__main__` 调用签名错误 | `torDo.py:77` | 直接运行该文件 `TypeError` | 与签名对齐，或补上桥接参数实现 |
| M6 | `start_tor` docstring 描述的五种网桥未实现 | `torDo.py:31-43` | 使用者被误导 | 更新文档或实现参数 |
| M7 | 时间戳精度为秒，同秒采集互相覆盖 | `action.py:61` | 极端情况下丢数据 | 时间戳加毫秒或加唯一后缀 |
| M8 | `duration/ttfb` 对 HTTP/1.1 相同、对 HTTP/2 不含 HEADERS 时间 | `http2decoder.py:135-138,253-277` | 指标解释需谨慎，跨协议不可直接比较 | 在输出中标注协议类型，或补充 HEADERS 时间 |
| M9 | Trojan 重建包时间戳为当前时间 | `TrojanDecoder.py:113-145`（scapy 默认 timestamp） | 内层 JSON 的时延指标无意义 | 从原始 pcap 继承时间戳 |
| M10 | `chrome.py:48` 直接索引 `disable_quic`，而另两个浏览器用 `.get` | 三处对比 | 删除配置键时 Chrome 路径直接崩溃 | 统一为 `.get(..., "false")` |
| M11 | 浏览器进程用 `pkill -KILL`/`-f` 粗暴清理 | `chrome.py:137`、`edge.py:235`、`firefox.py:72` | 可能误杀同机其他浏览器/驱动；影响同机其他测试 | 使用 Selenium 的 `quit()` + 精确 PID |
| M12 | `middlewares.__del__` 依赖 GC 关闭浏览器 | `middlewares.py:112-115` | 进程退出时可能残留浏览器进程 | 在 `spider_closed` 信号中显式关闭 |
| M13 | 依赖清单包含大量未使用包 | 1.4 节扫描结果 | 镜像体积与攻击面增大 | 精简 requirements |
| M14 | `requirements.txt` 未固定 `pyshark` 之外的 tshark 版本 | — | tshark 4.x 的字段名/`follow,tls,raw` 输出变化会破坏解码 | 在文档/启动检查中约束 tshark 版本 |

### 11.3 低优先级

| # | 问题 | 证据 |
| --- | --- | --- |
| L1 | `logger.py` 注释写 500MB，实际 50MB | `logger.py:17-20` |
| L2 | `action.py` 日志"爬取时间设为 X 分钟"实为秒/60 | `action.py:48` |
| L3 | `config.ini` 中 `depth`、`multisite_num` 无代码引用 | 全量 grep |
| L4 | `chrome.py:add_cookies` 依赖不存在的 `youtube_cookie.txt`，且无人调用 | `chrome.py:129-134` |
| L5 | `firefox.py` 顶部 53 行 JS 常量无引用 | `firefox.py:17-69` |
| L6 | `items.py`/`pipelines.py`/`SpiderSpiderMiddleware` 为空壳 | 对应文件 |
| L7 | `capture.py` 的 `__main__` 调用签名与函数不匹配 | `capture.py:82` vs `14` |
| L8 | `settings.py` 日志文件名月/日不补零 | `settings.py:45` |
| L9 | `kill_browsers` 未知类型用 `print` 而非 logger | `spider/__init__.py:23` |
| L10 | `config/current_docker_url_list_{background,monitor}.txt` 为遗留资产 | 全量 grep 无引用 |
| L11 | 仓库根存在 5.2GB `single_spider_traffic_260423.tar` | `ls -lh` |
| L12 | `Dockerfile` 基础镜像指向内网私有仓库 | `Dockerfile:1` |
| L13 | 未使用 `-U`/`--immediate-mode`，抓包尾部依赖缓冲刷新 | `capture.py:18-47` |

---

## 12. 排障手册

| 症状 | 可能原因 | 定位方法 | 处理 |
| --- | --- | --- | --- |
| 启动即 `PermissionError: logs/defult.log` | 日志文件被 root 创建 | `ls -l logs/` | `sudo chown -R $USER logs data` |
| 启动即 `KeyError: 'spider'` / `NoSectionError` | `config/config.ini` 缺失或段名不对 | `ls config/config.ini` | 按 6.1 节补齐 |
| 程序启动但没有任何新 pcap | 抓包权限不足 / 子进程失败 | `ls -l data/pcap/<host>/`；检查 `main.py:164` 返回码 | 配 `setcap` 或 sudo；改用 `sys.executable` |
| pcap 为 0 字节 | tcpdump 未起来（权限/网卡） | 手工执行同样的 tcpdump 命令看报错 | 修权限；显式指定 `-i` |
| pcap 有流量但没有 `<pcap>.log` | 浏览器未写 keylog 或归档前进程异常 | 看 `logs/defult.log` 中 `SSLKEYLOGFILE未找到` | 确认 `SSLKEYLOGFILE` 被继承；检查浏览器是否 headless 正常启动 |
| `<pcap>.json` 为空对象 `{}` | keylog 与 pcap 不匹配 / 没抓到解密后的 HTTP 层 | `tshark -r <pcap> -o tls.keylog_file:<log> -Y http` | 检查 keylog 是否被上一轮移动覆盖；确认 `is_decode=true` |
| JSON 中大量 `resources: []` | 只有握手或单向数据；埋点/心跳流 | 观察 `sni` 字段 | 正常现象；如需过滤可加最小资源数阈值 |
| HTTP/2 资源有 URL 但 `resource_data_size=0` | 响应体全在 HEADERS 后无 DATA，或走了 QUIC | Wireshark 打开 pcap 复核 | 确认 `disable_quic=true` |
| 浏览器启动失败（session not created） | driver 与浏览器版本不匹配 | `chromedriver --version` vs `google-chrome --version` | 使用 `bin/` 中成套版本重建镜像 |
| `FileNotFoundError: 未找到 Microsoft Edge 浏览器二进制文件` | Edge 未安装且未设环境变量 | `which microsoft-edge` | 安装 .deb 或设 `MSEDGE_BINARY` |
| `FileNotFoundError: 未找到 geckodriver` | geckodriver 不在 PATH | `which geckodriver` | 复制 `bin/geckodriver` 到 `/usr/local/bin` |
| xray 模式全部请求失败 | proxy 端口与 `xray.json` 不一致；`xray.json` 不存在 | `ss -lntp \| grep xray` | 对齐端口；`cp config/xray_trojan.json config/xray.json` |
| tor 模式长时间卡住后重试 | 未在 300s 内 `Bootstrapped 100%` | `cat logs/notice.log` | 检查网络与 `config/torrc` 网桥 |
| `TypeError: start_tor() takes 0 positional arguments` | 直接运行 `torDo.py` | `torDo.py:77` | 属已知缺陷，改为 `start_tor()` |
| Scrapy 只抓了首页 | `webnum=1`（默认） | `logs/url.log` 只有一行 | 调大 `webnum` 或设为 `-1` |
| 爬虫进入登录/注册页 | `exclude_keywords` 尾随空格缺陷 | `task.py:37` | 手工修代码或改关键字（加尾随空格） |
| 单站耗时远超预期 | 固定等待：3s + 30s + tor 60s | `main.py:266-270` | 按需调整 sleep |
| 解码时报 `pyshark`/`tshark` 相关异常 | tshark 未安装或版本差异 | `tshark --version` | 安装 tshark 并确认 ≥4.x |

---

## 13. 二次开发指南

### 13.1 代码规范（来自 `AGENTS.md`）

- PEP 8、4 空格缩进、UTF-8；
- 函数/变量/模块 `snake_case`，类 `PascalCase`；
- 副作用代码不放在 import 期，可执行逻辑放 `if __name__ == "__main__":`；
- 函数保持小而专注，统一用 `myutils/logger.py` 的 `logger` 输出。

> 现状与规范的偏差：`task_instance`、`config`、`logger` 都是 import 期副作用，新增模块时请避免继续扩散这种模式。

### 13.2 新增一种浏览器

1. 新建 `spider/<browser>.py`，实现 `create_<browser>_driver(...)` 与 `kill_<browser>_processes()`，返回值遵循现有约定（可返回 driver 或 `(driver, ssl_key_path)`）；
2. 在 `spider/__init__.py` 的 `BROWSER_CLEANERS` 注册清理函数；
3. 在 `middlewares.py:_create_browser_from_config` 的 `browser_creators` 注册创建函数；
4. 在 `main.py:181` 的 `valid_browsers` 加入新名称；
5. 保持 `SPIDER_MODE` 的代理语义（xray=http、tor=socks5），`disable_quic` 语义一致；
6. 在 Dockerfile 中安装对应浏览器与 driver。

### 13.3 新增一种协议解码

- direct 类（明文可解）：在 `tls_decoder/` 新增模块，输入 `(pcap, keylog)`，输出与 `TLSStreamDecoder.get_results()` 相同的字典结构，然后在 `main.py:_process_traffic_decoding` 增加分支；
- 隧道类：参考 `TrojanDecoder` 的三段式（tshark 剥离外层 → scapy 重建 → 复用内层解码器），新增协议时把"外层密钥来源"参数化；
- 无论哪种，都建议把结果 Schema 固定为 `{flow: {sni, resources[]}}`，下游才能通用。

### 13.4 建议补充的测试

仓库当前没有 `tests/`，`pytest` 也未加入依赖。建议优先级：

1. **P0** `tls_decoder` 离线单测：直接用 `test/test.pcap` + `test/sslkeys.log`，断言流数与关键资源（例如 `illinois.edu` 的 `http1-0`、`cdnjs.cloudflare.com` 的资源数），防止解码回归；
2. **P1** 配置解析单测：给 `Task` 传入临时目录，覆盖空行/注释/`running.json` 缺失/关键字过滤；
3. **P1** `FlowKey` 方向无关性单测；
4. **P2** 文件名生成单测（`_normalize_url_for_path`、pcap 命名）；
5. **P2** 浏览器 driver 解析函数的单测（monkeypatch `os.environ`/`shutil.which`）。

测试命令（约定）：

```bash
pytest -q
```

### 13.5 提交规范

`feat:` / `fix:` / `refactor:` / `docs:` / `chore:`；一次提交只做一类改动；PR 需说明配置变更、验证命令与产物路径。

---

## 14. 安全与合规

### 14.1 敏感资产

| 资产 | 位置 | 风险 |
| --- | --- | --- |
| TLS 会话密钥 | `test/sslkeys.log`、`data/**/*.log` | 可解密对应历史流量 |
| 原始抓包 | `data/pcap/**` | 含完整明文 URL、Cookie、响应体 |
| 代理凭据 | `config/xray_*.json`（含服务器地址与密码） | 凭据泄露 |
| 网络拓扑 | `config/config.ini`（`ip_addr`/`name`/`site`） | 暴露节点位置与角色 |
| 完整环境备份 | `single_spider_traffic_260423.tar`（5.2GB） | 可能包含以上全部 |
| Edge netlog | `/tmp/netlog.json` | 全量网络事件 |

### 14.2 建议

1. 只在不含真实凭据的样例上做演示；分享截图/日志前先脱敏（`sni`、IP、URL、密码）；本文档中的节点 IP、代理服务器与端口示例已统一替换为 `<node-ip>` / `<proxy-host>` 等占位符；
2. 不要把 `config/`、`data/`、`logs/`、`test/`、`*.tar` 提交或推送到任何远程；
3. 生产/实验环境使用后的 keylog 与 pcap 应及时加密归档或销毁；
4. 抓包与代理仅在你**明确获授权**的网络与目标上执行；本框架会加载真实站点并抓取其全部流量；
5. 对外分发镜像前替换掉 `config/xray_*.json` 中的真实服务器与密码，并确认 `Dockerfile` 中的私有镜像地址已替换。

---

## 15. 附录

### 15.1 关键符号索引

| 符号 | 位置 | 作用 |
| --- | --- | --- |
| `browser_action()` | `main.py:167` | 主循环 |
| `_save_sslkey_files()` | `main.py:19` | 密钥归档 |
| `run_action_script()` | `main.py:161` | 子进程启动 |
| `start_spider()` | `action.py:35` | Scrapy 运行 |
| `traffic()` | `action.py:57` | 抓包 + 路径生成 |
| `capture()` | `capture.py:14` | tcpdump 启动 |
| `task_instance` | `task.py:61` | 任务单例 |
| `Spider` | `spiders/trace.py:12` | 链接发现 |
| `SpiderDownloaderMiddleware` | `middlewares.py:70` | 浏览器中间件 |
| `create_chrome_driver()` / `create_edge_driver()` / `create_firefox_driver()` | 各自模块 | 浏览器创建 |
| `kill_browsers()` | `spider/__init__.py:13` | 进程清理分发 |
| `FlowKey` | `flow_key.py:5` | 方向无关流键 |
| `TLSStreamDecoder` | `http2decoder.py:11` | 解码器 |
| `TrojanDecoder` / `action()` | `TrojanDecoder.py:19,238` | Trojan 解码 |
| `start_tor()` / `close_tor()` | `torDo.py:31,64` | Tor 生命周期 |
| `project_path` | `myutils/__init__.py:3` | 仓库根路径 |
| `SPIDER_MODE`、`config` | `myutils/config.py:7,13` | 全局配置 |

### 15.2 配置速查模板（脱敏）

```ini
[information]
name = <node-name>
protocal = direct          ; xray/tor 模式下为实际协议名；trojan 才会触发 Trojan 解码
site = <region-label>
ip_addr = <proxy-server-ip>   ; 仅 xray 抓包过滤用

[proxy]
host = 127.0.0.1
port = 10809               ; 必须与 Xray/Tor 监听端口一致

[spider]
browser = firefox          ; chrome | edge | firefox
time_per_website = 30
download_delay = 60
mode = direct              ; direct | xray | tor
scroll = true
scroll_num = 3
webnum = 1                 ; -1 表示不限
disable_quic = true
is_decode = true
```

### 15.3 术语表

| 术语 | 含义 |
| --- | --- |
| keylog / SSLKEYLOGFILE | 浏览器导出的 TLS 会话密钥文件，用于离线解密 |
| SNI | TLS ClientHello 中的目标域名 |
| FlowKey | 方向无关的四元组（IP+端口）流标识 |
| TTFB | 首字节时间，此处指请求帧到首个响应数据帧的间隔 |
| 外层/内层 | 代理隧道：外层是浏览器↔代理的 TLS，内层是代理↔目标站的 TLS |
| direct / xray / tor | 三种采集模式，分别对应直连、Xray 代理、Tor 代理 |
| pcap | tcpdump 生成的原始抓包文件 |

### 15.4 对既有文档的勘误

以下内容来自 `技术文档.md` / `README.md`，与实际代码不符，使用时请以本文档为准：

| 旧文档描述 | 实际情况 | 证据 |
| --- | --- | --- |
| JSON 顶层键为 `"['ip', port, 'ip', port]"`（Python list 形式） | 实际是 `Flow(ip:port <-> ip:port)` 字符串 | `flow_key.py:30-32` |
| JSON 含 `client_ip` 字段 | `client_ip` 只用于内部方向判断，不写入结果 | `http2decoder.py:336` |
| pcap 命名包含 URL hash 或 `traffic_` 前缀 | 实际是 `<协议>_<时间>_<name>_<site>_<host>.pcap` | `action.py:65-68` |
| URL 列表要求带 `http(s)://` 前缀 | 实际是纯主机名，程序自行拼 `https://` | `task.py:53` |
| 存在 `[logging]` 配置段 | 代码只读 `[information]`/`[proxy]`/`[spider]` | `config.py`、全量 grep |
| `time_per_website` 默认 300、`download_delay` 默认 3 | 当前 `config.ini` 为 30 / 60，且代码无默认值 | `config/config.ini` |
| `技术文档.md` 指导安装 Playwright 浏览器，暗示其为运行依赖 | 源码中无任何 Playwright 引用，实际路径是 Selenium（`README.md` 也已说明这一点） | 全量 import 扫描 |
| SSL 密钥日志示例含 `CLIENT_RANDOM` 行 | TLS1.3 场景实际为 `*_TRAFFIC_SECRET_*` 行 | `test/sslkeys.log` |
| 解码 `duration` 对 HTTP/1.1 与 HTTP/2 语义相同 | HTTP/1.1 下 `duration == ttfb`；HTTP/2 的 HEADERS 时间不计入 TTFB | `http2decoder.py:135-138` |

---

*文档结束。如需与本仓库既有材料对照：功能与用法看 `README.md`，架构决策与历史分析看 `项目分析报告.md`，工程规范看 `AGENTS.md`；本文档以当前源码行为为准。*
